{% extends 'adminLayout.html.twig' %}

{% block content %}

    <h1>Liste des utilisateurs</h1>
    <table class="table table-hover">
        <thead class="thead-dark">
        <tr>
            <th>Nom</th>
            <th>Prénom</th>
            <th>Mail</th>
            <th>Action</th>
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td><h5>{{ user.name }}</h5></td>

                <td><h5>{{ user.firstname }}</h5></td>

                <td><h5>{{ user.mail }}</h5></td>


                <td>
                    <a type="button" href="/admin/user/delete/{{ user.id }}" class="btn btn-outline-warning"><i class="fas fa-trash-alt"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}