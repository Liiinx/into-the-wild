{% if(session["flash"]) %}
    {% for sessionKey, sessionValue in session["flash"] %}
        <div id="wcs-flasher" class="alert alert-{{ sessionKey }}" role="alert">
            <b style="display: block; text-align: center;" class="animated bounceInRight">{{ sessionValue }}</b>
        </div>
    {% endfor %}
{% endif %}